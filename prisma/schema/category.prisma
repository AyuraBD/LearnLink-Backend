model Category {
  id        String         @id @default(uuid())
  name      String         @unique
  description String?

  tutors    TutorProfile[] @relation("TutorCategories")

  createdAt DateTime       @default(now())
}